<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.review.manage.reviewClass.mapper.ReviewClassMapper">
    <!--<update id="updateByClassId" parameterType="com.review.manage.reviewClass.entity.ReviewClass">
        UPDATE review_class
        SET sort_id = #{sortId},
            title = #{title},
            class_desc = #{classDesc},
            guide = #{guide},
            charge = #{charge},
            org_price = #{orgPrice},
            dicount_price = #{dicountPrice},
            banner_img = #{bannerImg},
            video_analysis = #{videoAnalysis}
        WHERE
            class_id = #{classId}
    </update>-->
    <delete id="deleteClassByClassId" parameterType="java.lang.String">
        DELETE
        FROM
            review_class
        WHERE
            class_id = #{classId}
    </delete>

    <delete id="deleteByMainId" parameterType="java.lang.String">
        DELETE
        FROM
            review_question_class
        WHERE
            class_id = #{classId}
    </delete>
    <select id="getClassIds" parameterType="Long" resultType="String">
        select class_id as classId from review_class_tenant_conf where tenant_id = #{tenantId}
    </select>
</mapper>